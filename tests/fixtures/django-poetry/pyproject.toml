[tool.poetry]
name = "myproject"
version = "1.0.0"
description = "A Django project using Poetry"
authors = ["dev@example.com"]

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.2"
djangorestframework = "^3.15"
celery = "^5.3"

[tool.poetry.dev-dependencies]
pytest = "^8.0"
pytest-django = "^4.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
